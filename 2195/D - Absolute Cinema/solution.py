"""D. Absolute Cinema — recover a[1..n] from f(x) = sum_i a_i * |i - x| at x = 1..n. Derivation:  d_x = f(x+1) - f(x) = 2*S_x - T  =>  a_k = (d_k - d_{k-1})/2 for k = 2..n-1.  f(1) = sum_{i=2}^n (i-1)*a_i  =>  a_n from known middle a_i.  a_1 - a_n = (d_1 + d_{n-1})/2  =>  a_1.Special case n == 2: f(1) = a_2, f(2) = a_1.""" import sys  def solve() -> None:    data = list(map(int, sys.stdin.buffer.read().split()))    it = iter(data)    t = next(it)    out_lines = []    for _ in range(t):        n = next(it)        f = [next(it) for _ in range(n)]         if n == 2:            # f[0]=f(1)=a_2, f[1]=f(2)=a_1            a = [f[1], f[0]]            out_lines.append(" ".join(str(x) for x in a))            continue         d = [f[i + 1] - f[i] for i in range(n - 1)]        a = [0] * n        for k in range(1, n - 1):            a[k] = (d[k] - d[k - 1]) // 2         s = sum(j * a[j] for j in range(1, n - 1))        a[n - 1] = (f[0] - s) // (n - 1)        a[0] = a[n - 1] + (d[0] + d[n - 2]) // 2         out_lines.append(" ".join(str(x) for x in a))     sys.stdout.write("\n".join(out_lines))  if __name__ == "__main__":    solve()